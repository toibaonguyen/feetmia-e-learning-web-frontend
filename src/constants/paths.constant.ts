const PATH_HOME: string = "/";

const PATH_COURSES = {
    BASE_URL: "/courses",
    QUERY: {
        search: "search",
        category: "category",
        skill: "skill",
        role: "role"
    }
}

const PATH_INSTRUCTORS = {
    BASE_URL: "/instructors",
}

const PATH_SIGN_UP: string = "/auth/sign-up";

const PATH_LOG_IN: string = "/auth/log-in";

const PATH_PROFILE: string = "/profile/";

const PATH_PURCHASES_HISTORY: string = "/purchases-history/";

const PATH_SETTINGS: string = "/settings/";

const PATH_CART: string = "/cart/";

const PATH_NOTIFICATIONS: string = "/notifications/";


export {
    PATH_HOME,
    PATH_COURSES,
    PATH_SIGN_UP,
    PATH_LOG_IN,
    PATH_CART,
    PATH_PROFILE,
    PATH_PURCHASES_HISTORY,
    PATH_SETTINGS,
    PATH_NOTIFICATIONS,
    PATH_INSTRUCTORS
};